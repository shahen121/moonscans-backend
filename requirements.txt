# متطلبات تطبيق Mangawy API
# تم التحديث في: 2026-01-06
# ==========================================

# FastAPI والمكونات الأساسية
# -----------------------------------------
fastapi==0.115.6                    # إطار عمل FastAPI الحديث مع تحسينات الأداء
uvicorn[standard]==0.34.0           # خادم ASYNC عالي الأداء مع دعم WebSocket
python-multipart==0.0.20            # دعم تحميل الملفات ومعالجة Forms

# طلبات HTTP وإدارة العملاء
# -----------------------------------------
httpx==0.28.1                       # عميل HTTP حديث وقوي مع دعم HTTP/2
aiofiles==24.1.0                    # ملفات غير متزامنة لتحسين الأداء

# معالجة HTML و Web Scraping
# -----------------------------------------
beautifulsoup4==4.12.3              # مكتبة Scraping القوية لاستخراج البيانات من HTML
lxml==5.3.0                         # معالج XML/HTML سريع وقوي

# التحقق من البيانات والـ Validation
# -----------------------------------------
pydantic==2.10.3                    # مكتبة قوية للتحقق من البيانات وإدارتها (V2)
pydantic-settings==2.7.1            # إدارة الإعدادات من ملفات ENV

# أدوات مساعدة وخدمات
# -----------------------------------------
python-dotenv==1.0.1                # تحميل متغيرات البيئة من ملف .env
python-dateutil==2.9.0              # معالجة التواريخ والأوقات المتقدمة
orjson==3.10.12                     # مكتبة JSON سريعة جداً مكتوبة بـ Rust

# أدوات التطوير والاختبار (اختياري - قم بإلغاء التعليق عند الحاجة)
# -----------------------------------------
# pytest==8.3.4                      # إطار عمل الاختبار الرئيسي
# pytest-asyncio==0.25.0             # دعم الاختبارات الغير متزامنة
# pytest-cov==6.0.0                  # تغطية الاختبارات
# black==24.10.0                     # منسق الكود الآلي (Code Formatter)
# flake8==7.1.1                      -# مدقق جودة الكود (Linter)
# mypy==1.14.1                       -# مدقق الأنواع (Type Checker)
# isort==5.13.2                      -# منظم الـ Imports

# أدوات مراقبة الأداء (اختياري)
# -----------------------------------------
# prometheus-fastapi-instrumentator==7.0.0  # مونتيرنج وـ Metrics
# sentry-sdk[fastapi]==2.19.2        # تتبع الأخطاء والـ Performance

# أدوات الـ Cache (اختياري)
# -----------------------------------------
# redis==5.2.1                       # قاعدة بيانات Redis للـ Cache
# aioredis==2.0.1                    # عميل Redis غير متزامن
# fastapi-cache2==0.2.2              # نظام Cache متكامل مع FastAPI
